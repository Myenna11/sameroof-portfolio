{
  "name": "sameroof",
  "version": "0.1.0",
  "private": true,
  "description": "Same Roof: multi-provider agent runtime — credential broker, execution gateway, message coordinator. Root package only manages workspaces.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present"
  },
  "license": "MIT"
}
