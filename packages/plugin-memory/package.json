{
  "name": "@sameroof/plugin-memory",
  "version": "0.1.0",
  "private": true,
  "description": "同屋插件：房间记忆（写入脱敏、召回、亲笔保护、fact_key 版本、审核队列）",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
