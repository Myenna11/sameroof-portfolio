const C='sameroof-v2',STATIC=new Set(['/','/index.html','/manifest.json','/sw.js']);
self.addEventListener('install',e=>e.waitUntil(caches.open(C).then(c=>c.addAll([...STATIC]))));
self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==C).map(k=>caches.delete(k))))));
self.addEventListener('fetch',e=>{const u=new URL(e.request.url);if(e.request.method!=='GET'||u.origin!==location.origin||!STATIC.has(u.pathname))return;e.respondWith(fetch(e.request).then(r=>{if(r.ok){const cp=r.clone();caches.open(C).then(c=>c.put(e.request,cp))}return r}).catch(()=>caches.match(e.request)))});
self.addEventListener('push',e=>{let p={title:'同屋',body:'家里有人叫你',url:'/'};try{p={...p,...e.data.json()}}catch{}e.waitUntil(self.registration.showNotification(String(p.title).slice(0,80),{body:String(p.body).slice(0,240),data:{url:p.url||'/'},tag:'sameroof-message',renotify:true}))});
self.addEventListener('notificationclick',e=>{e.notification.close();const url=new URL((e.notification.data&&e.notification.data.url)||'/',self.location.origin).href;e.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list)if(c.url.startsWith(self.location.origin)&&'focus'in c)return c.focus();return clients.openWindow(url)}))});
