# Fictional agent. Mock upstream requires SAMEROOF_ENABLE_MOCK=1.
# See docs/ROOM_SPEC.md for the complete configuration schema.
schema_version: 1
id: resident_example_01
name: example
species: agent
runtime: broker-direct
model:
  provider: mock
  id: mock-chat
  auth: {mode: broker, credential: mock-cheap}
plugins: [memory, handover, living-room]
heartbeat: {enabled: false}
